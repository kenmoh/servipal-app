import React from "react";
import { StyleSheet, Text, View } from "react-native";

const extras = () => {
  return (
    <View className="flex-1 bg-background">
      <Text className="text-primary">extras</Text>
    </View>
  );
};

export default extras;

const styles = StyleSheet.create({});
